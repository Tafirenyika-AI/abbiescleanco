# Implementation Plan / Backlog

Mapped to the blueprint's own phase structure (§9), reconciled against what's actually already built (`docs/REPO_AUDIT.md`). Each item has an owner-dependency flag: **[code]** = buildable now with no new credential/decision from the owner, **[creds]** = blocked on a credential the owner needs to enter, **[decision]** = blocked on a business/legal decision only the owner can make.

## Phase 0 — Audit, architecture, baseline (this session)

Done: `docs/REPO_AUDIT.md`, `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, `docs/SECURITY.md`, `docs/INTEGRATIONS.md`, `docs/OPERATIONS.md`, `docs/DECISIONS.md`, this backlog. Baseline confirmed: 35/35 tests passing, lint at documented baseline (1 pre-existing intentional error, 2 non-correctness warnings), production build clean.

## Phase 1 — CRM + lead-to-quote vertical slice

**Status: already built and live-verified in prior sessions** (`docs/REPO_AUDIT.md` §3). The one real correctness gap found in this area — booking double-booking under concurrency — is fixed this session (`docs/DECISIONS.md` D-1), live-verified with real concurrent requests against the database.

Remaining smaller items in this phase's spirit, not yet done:
- ~~**[code]** Capture UTM parameters (`utm_source`/`utm_medium`/`utm_content`, referrer)~~ **Done 2026-09-22** (`6543779`) — `Lead.utmSource`/`utmMedium`/`utmContent`/`referrer`, captured on the estimate form and visible to admins in the lead detail drawer's new Attribution section.
- ~~**[code]** Store a computed dedup fingerprint (`hash(email, phone, serviceId)`) on `Lead`~~ **Done 2026-09-22** (`4966a0c`) — `Lead.dedupFingerprint`, indexed, now what duplicate-submission detection actually queries against. Fixed a real bug along the way: the database path never filtered by service, so two different services requested by the same contact within 10 minutes were wrongly deduped into one lead.
- **[decision]** Commercial RFP tracker (§3.1) — no evidence this business currently pursues commercial RFPs; don't build speculatively.

## Phase 2 — Booking, capacity, confirmations, reminders, portal, auth

**Status: already built and live-verified**, including the customer portal's full self-service management (accept/decline quotes, cancel/reschedule, pay, attach notes/photos/videos) added 2026-09-19. Capacity-safety bug fixed this session (D-1).

Remaining gaps:
- **[decision]** Recurring service enrollment — `RecurringSchedule` model exists but is entirely unused; `recurringDiscountRates` exist in the pricing engine but are switched off pending owner-approved rates (a prior, deliberate decision, not an oversight). Needs the owner to confirm actual recurring pricing before this is worth building out.
- ~~**[code]** `middleware.ts` defense-in-depth for admin routes~~ **Done 2026-09-21** — `src/proxy.ts` (Next 16 renamed the convention from `middleware.ts`; migrated via `@next/codemod`). See `docs/SECURITY.md` §2 and `docs/DECISIONS.md` D-8.
- ~~**[code]** The equivalent backstop for `/api/account/*`~~ **Done 2026-09-21** — `src/proxy.ts` now covers both surfaces (see `docs/DECISIONS.md` D-8, updated).

## Phase 3 — Payments, invoices, financial reporting

**Status: partial.** Payments (Stripe + 6 manual methods) and reports exist.

- **[creds] Stripe key entered 2026-09-23, tested against a real Checkout session — blocked one step short**: the key authenticates, but Stripe itself rejects session creation with "you must set an account or business name at https://dashboard.stripe.com/account." Not a code issue; owner needs to set that in their Stripe dashboard, then it should just work (code already proven correct otherwise — real Payment row + real PaymentIntent id would be created).
- ~~**[decision]** Whether to build a real `Invoice`/`InvoiceLine` model + PDF generation, or integrate with QuickBooks/similar~~ **Resolved 2026-09-24** — owner supplied a full "Abbie Finance" spec explicitly choosing a hand-built system over a QuickBooks/Xero sync ("the objective is NOT to recreate QuickBooks"). **Phase 1 built and live-verified**: `/admin/finance` overview dashboard (today/month/year, real revenue/collected/expenses/profit/outstanding, revenue-vs-expenses trend, expenses-by-category and revenue-by-service donuts); `Invoice.status` is now a real derived enum (`UNPAID/PARTIALLY_PAID/PAID/OVERDUE/REFUNDED/CANCELLED`, computed live from linked payments, never hand-set); `Payment` gained `tipAmount`/`processingFeeAmount`/`refundAmount`/`refundedAt`/`refundReason`/`provider`; a real refund flow (`recordRefund`, admin-approved, audited) with a working UI on `/admin/payments`; `Expense` gained a `bookingId` link + 9 finer categories; 9 new granular `FINANCE_*` permissions (existing `VIEW_REPORTS` holders auto-granted `FINANCE_VIEW` in the migration so nobody lost access). Also fixed two real pre-existing bugs found along the way: the Stripe webhook wasn't idempotent for its notification/receipt-email side effects (a retried webhook delivery would re-notify/re-email), and Payments/Invoices/Expenses pages had no page-level permission gate at all (only the API routes did) -- both fixed.
- ~~**Deferred**: bank CSV import/reconciliation~~ **Done 2026-09-24** — `/admin/finance/banking`: CSV upload (tolerant of common bank export column-name variants, single-Amount or separate Debit/Credit columns), real duplicate-import detection (re-uploading the same statement skips rows already imported, hashed on date+amount+description), and suggested matches against real unmatched Payments/Expenses within a 4-day window by exact amount -- admin always confirms/rejects, nothing auto-reconciles. Manual picker for anything with no suggestion. Live-verified 17/17 including the exact suggestion-matching and duplicate-skip behavior against real data.
- ~~**Deferred**: marketing ROI (blocked on Marketing studio existing first)~~ **Unblocked and done 2026-09-24** — see Phase 7 below; `/admin/marketing` now computes real campaign ROI, which Finance's own dashboard can read once a "marketing spend" card is added there (not yet wired into `/admin/finance` itself, but the underlying data now exists).
- **Deferred (documented roadmap, not built in Phase 1)**: receipt scanner (reuses the existing `photoEstimate.ts` vision pattern), subcontractor payables (blocked on the Workforce module existing first), job profitability, tax record center, financial document vault, an Abbie-chat Finance assistant (reuses `aiAgent.ts`), and financial alerts. Coming-Soon pages exist for Subcontractors/Tax records/Documents under the Finance nav group, each honestly stating what it needs first.
- **[code]** Receivables aging report, distinct "billed vs. collected vs. accounting profit" views (blueprint §3.6) — Finance's `/admin/finance` overview now separates Revenue (accrual) from Collected (cash) and shows Outstanding invoices, but a dedicated aging report (30/60/90 days overdue) isn't built yet.

## Phase 4 — Cleaner/subcontractor/property-manager operations, CleanPass

**Status: not started beyond the flat `TeamMember`/`Expense` roster.** This is a genuinely large, new module:
- **[decision]** Employee vs. subcontractor classification, insurance/licensing requirements, pay structure — all require the owner's actual business practice and likely professional (legal/accounting) review before any code models it, per the blueprint's own instruction (§3.5, §7). Building a subcontractor-agreement/classification system ahead of that would risk encoding wrong assumptions into the business's actual legal exposure.
- **[code, once classification is settled]** Cleaner login + assigned-job view, job-completion checklist, CleanPass report generation, property-manager portal.

## Phase 5 — Abbie Chat (read-only navigation, then confirmed actions)

**Status: levels 1 & 2 done for all three roles (2026-09-22, `2c55204` admin, `15c0955` customer, `076d4d5` guest) — level 3 (confirmed actions) not started.** Three parallel assistants, one pattern, each scoped to what that role should see, per the blueprint's own "guest ... never private account data" / "authenticated customers see only own records" / admin scoped by permission:
- **Admin** — `src/lib/server/assistant.ts` + `POST /api/admin/assistant`, the "Ask Abbie Assistant" bar in the admin header. 7 tools (new leads, pipeline value, conversion rate, net revenue, overdue payments, bookings this week/today, unassigned bookings) authorized through `requireAdmin()`. Live-verified 11/11.
- **Customer** — `src/lib/server/customerAssistant.ts` + `POST /api/account/assistant`, a floating widget on `/account`. 5 tools (next appointment, balance/pending payments, active bookings, quotes awaiting decision, open requests) scoped to `requireCustomer()`. Live-verified 10/10, including a real two-customer isolation test (a second signed-up customer got their own 0, not the first customer's data).
- **Guest** — `src/lib/server/guestAssistant.ts` + `POST /api/guest-assistant`, a floating widget site-wide (hidden on `/account`, which has its own). 8 tools with no identity concept at all (service areas, real admin-configured pricing, hours, contact, WhatsApp, services offered, pet policy, "get an estimate" navigates to the real form) — nothing private to leak by construction. Public and rate-limited (30/10min/IP). Live-verified 11/11.

All three: level 1 (§4 typed route registry + deterministic intent matching) reuses `allNavItems` from `src/lib/admin/nav.ts` for the admin case; no separate registry needed elsewhere since customer/guest tools don't need cross-page navigation the same way. Level 2 (typed, authorized data tools) is real store-function calls, zero model calls anywhere — deterministic keyword matching only, per the blueprint's own §6 instruction not to spend an AI call on navigation or arithmetic a database query already answers.
- ~~**[code]** Level 3 (propose/execute actions after confirmation)~~ **Started 2026-09-22** (`311c0d7`) — admin assistant only so far. "Mark ACM-26-XXXXXX as contacted/confirmed/scheduled/etc." resolves to a `type: "confirm"` result (never executes on the same request); the UI shows explicit Confirm/Cancel; only a confirmed click POSTs to `/api/admin/assistant/execute`, which re-validates the lead's current state, applies `updateLeadStatus` (same function and `MANAGE_LEADS` permission gate the manual status dropdown uses), and writes the same audit log entry. Live-verified 19/19 (propose doesn't mutate, execute does + is real/persisted/audited, unauthenticated execute is rejected, bogus/unknown reference and stale leadId fail cleanly, already-at-that-status short-circuits) + a real Playwright browser pass. **Not yet done**: customer/guest assistant level 3 (no actions proposed for those roles yet), and richer admin action types beyond a lead status change (e.g. "create a quote from lead X", "assign booking Y to \<cleaner\>").
- **[decision/creds]** A real model-backed layer (natural-language queries beyond the fixed keyword sets) needs the Anthropic key already flagged elsewhere in this plan, plus a decision on how much free-text flexibility to allow before it's worth the cost/complexity.

## Phase 6 — Abbie Vision (upload/processing/confirmation)

**Status: partial.** The photo-estimate feature (`/estimate/photos`) already does direct-to-storage upload, MIME/size validation, a structured-output pipeline (condition/add-ons/supplies via a tool-call schema, not free text), and keeps price generation deterministic and separate from the model — this is most of the hard part of §3.4 already done.

Missing from the full §3.4 spec:
- ~~**[creds]** Anthropic API key~~ **Done 2026-09-23** — owner's first key was org-scoped (blocked); replaced with a workspace-scoped key, real vision call succeeds. Verified against two real gallery photos (a dirty oven, a cooktop): accurate condition assessment, sensible supply lists, and genuinely useful model reasoning (one response correctly flagged that only one room was photographed despite a whole-home job description, and recommended confirming scope with the customer before booking — not a canned response). Verified through both the raw API and the full customer-facing `/estimate/photos` UI including the review/approve gate. AI photo estimation is now fully live.
- ~~**[code]** Voice/audio upload~~ **partially done 2026-09-22** (`4966a0c`) — customers can attach a voice note (WAV/MP3/OGG/M4A, magic-byte validated, 15MB cap) to a request/booking thread, rendered as a native audio player for both customer and admin views. **Transcription is still not built** — the Anthropic key now works (see below), so this is a real, buildable-now **[code]** item whenever it's prioritized, not creds-blocked anymore.
- ~~**[code]** The explicit "customer reviews/edits/approves the structured scope before it's shared with the crew" step~~ **Done 2026-09-22** (`6543779`) — the customer now sees the same supplies/crew-notes the AI generated, can add their own corrections, and must explicitly choose to share before `linkPhotoEstimateToLead` posts anything to the crew; declining, or never reviewing at all, safely results in no crew note (photos still attach either way).
- **[decision]** After-service photo QA — needs owner sign-off on what claims (if any) are made about it, since the blueprint is explicit that images must never be claimed to prove sanitation.

## Phase 7 — Marketing integrations, approvals, attribution

**Status: mostly not started**, but one real piece shipped 2026-09-22 (`517b046`): **Prospecting** (`/admin/prospecting`) — outbound discovery of potential customers, distinct from every other feature so far which only handles inbound leads. Owner clarified the actual want: an AI that finds leads (property managers, realtors, local businesses, new homeowners), not just processes ones that already came in.
- Answered via `AskUserQuestion` (owner's explicit choices): data source = web/business-directory search; targets = property managers, realtors, local businesses, AND new homeowners (broad); outreach = always human-approved before sending, never auto-sent.
- **[code, done]**: Google Places (New) Text Search integration, using the `googleMapsApiKey` field that already existed in Settings but was unwired until now — now fully live with a real key (2026-09-23). Dedupes by Google's own place id. Deterministic template-based outreach drafting (no model call — deliberately kept this way even now that the Anthropic key works, for cost/predictability; template drafts personalize only on real prospect/business data, never invented). A prospect can be marked "do not contact," which hard-blocks any future send. Includes a CAN-SPAM opt-out line in every draft.
- ~~**[creds]** Google Maps/Places API key~~ **Done 2026-09-23** — owner enabled "Places API (New)" on the project, then fixed the key's own API restrictions (it was blocking the Places method even after the project-level API was enabled — two separate settings on Google's side, not a code issue either time). Real search now returns real businesses (verified: 20 real property management companies in Spokane Valley with real names/addresses/phones/websites, live dedup confirmed on re-search). Prospecting is now fully live, not just code-complete.
- **Deliberately NOT built**: new-homeowner discovery specifically. Google Places is a business/POI directory — it has no concept of "who just bought a house." That needs a different data source entirely (property/real-estate transaction records, e.g. an MLS feed or a paid provider like ATTOM/Estated), which is its own **[decision]** (which provider, at what cost) before any **[creds]**/**[code]**. Told the owner this rather than silently only covering 3 of the 4 requested target types.
- **Deliberately NOT built**: SMS/voice outreach. TCPA's consent bar for calls/texts is meaningfully stricter than email's CAN-SPAM rules, especially for cold outreach to numbers that never opted in — this deserves the owner's own decision (and possibly legal input) before any code assumes it's fine, rather than defaulting to "the human approves it so it must be OK."
- ~~**[code]** Marketing spend/ROI tracking + content planner~~ **Done 2026-09-24** — `/admin/marketing` ("Marketing studio", `MANAGE_CONTENT`-gated, was a Coming-Soon placeholder). `MarketingCampaign` (channel, status, manually-entered `spend`, and a unique `utmCampaign` value) matched against the real `Lead.campaign` field every lead already captures from the public estimate form's UTM params — leads/customers/revenue attributed by exact match only, never a guess, and shown as real `—`/0 (not fabricated numbers) when a campaign has no UTM value set or no matching leads yet. Revenue is real collected payments (PAID, net of refunds) on bookings tied to those leads. `MarketingPost` gives a draft → approve → posted content planner (same "AI/admin prepares, a human approves" pattern used everywhere else in this app). Live-verified 33/33 (permission gating both at the API and page level, real ROI math against real seeded leads/payments, duplicate-UTM rejection, the full draft/approve/posted/cancel state machine including illegal transitions rejected, audit log rows, delete-blocked-while-posts-attached) plus a real-browser Playwright pass creating a campaign through the actual UI form.
- ~~**[code]** Automated social/ad publishing (owner request 2026-09-24: "not run this business automated... minimize manual work")~~ **Meta (Facebook + Instagram) fully done same day; Google Ads + TikTok OAuth connect done, their publish/campaign-creation execution is next.** New `SocialConnection` model stores real OAuth-obtained account tokens (Page/IG business account/Ads customer id/TikTok advertiser id), connected via real "Connect X" OAuth flows under `/api/admin/marketing/connections/{meta,google-ads,tiktok}/{start,callback}` (CSRF-protected via a signed `state` param). Each platform's app-level credentials (App ID/secret, OAuth client id/secret, developer token) are entered in Settings → Integrations, same DB-with-env-fallback pattern as every other credential in this app. An approved `MarketingPost` with a connected account gets a real **Publish now** action (`publishPost()` in `marketingStore.ts`) instead of the manual **Mark posted** fallback, which stays available for any channel/post with no connection.
  - **Meta**: `src/lib/server/social/meta.ts` — real long-lived-token exchange, real Page + linked Instagram Business account discovery via `/me/accounts`, real `POST /{page}/feed` or `/{page}/photos` for Facebook, and the real two-step `/media` → `/media_publish` flow for Instagram (requires an image; the post form now supports uploading one via the existing `/api/admin/upload` pipeline). **Live-verified against Facebook's real Graph API**, not mocked: a deliberately fake Page token was correctly rejected with Facebook's own real error ("Invalid OAuth access token — Cannot parse access token"), and the post correctly stayed `APPROVED` (never silently marked `POSTED`) with the real error text stored and shown in the UI. Self-use exemption applies (no Meta App Review needed for the business's own Page, per Meta's own documented exception) — see `docs/SOCIAL_ADS_SETUP.md` for the exact app-creation steps and required redirect URI.
  - **Google Ads**: `src/lib/server/social/googleAds.ts` — real OAuth2 (offline access, refresh token) + real `customers:listAccessibleCustomers` call proving the developer token/app work. **Campaign creation/management (budgets, ad groups, ads, targeting) is deliberately not built yet** — writing that blind against zero real accounts would risk subtly wrong code; sequenced as the next step once the owner has a real connection to build/test against.
  - **TikTok**: `src/lib/server/social/tiktok.ts` — real OAuth2 connect + user-info fetch. **Actual video publish via the Content Posting API not built yet** — pending TikTok's own approval of the `video.publish` audited scope on the owner's app, which isn't guaranteed on a first submission.
  - Attempting to publish through a channel with no real publish implementation (Google Ads/TikTok today) returns an honest "isn't built yet" error rather than silently succeeding — verified this explicitly rather than assuming it.
  - `docs/SOCIAL_ADS_SETUP.md` (new) gives the owner exact, copy-pasteable steps for all three developer apps, including the precise redirect URI each one needs (built from `NEXT_PUBLIC_SITE_URL`, which must be confirmed set to `https://abbiescleanco.vercel.app` in Vercel's env vars — flagged explicitly since an unset/wrong value would silently send the OAuth redirect to the wrong domain).
  - Live-verified 36/36 against the real production DB + a real local server (not mocked, except unreachable per-platform final publish for Google Ads/TikTok which have no real account to test against yet): permission gating (API + OAuth start routes), OAuth start URLs correctly built once fake app credentials are saved (right `client_id`, `redirect_uri`, scopes, signed `state`) and correctly redirect to a `connectError` when credentials are missing, callback CSRF protection (tampered/missing `state` rejected cleanly), connections never expose access tokens to the client, the full publish-gating state machine (must be `APPROVED`, must have a connection, a real failed Graph API call leaves the post `APPROVED` with a stored `publishError` rather than faking success), the manual `Mark posted` fallback still works for unconnected channels, and real `AuditLog` rows for connect/disconnect/publish-failed actions.
  - **Noted, not chased**: attempting to check whether `NEXT_PUBLIC_SITE_URL` is actually set correctly on the live Vercel deployment via the Vercel CLI required an interactive device-code login this session unexpectedly completed against some existing credential on this machine — stopped short of linking the project non-interactively rather than proceeding on an ambiguous auth state; the owner should verify that env var directly in the Vercel dashboard before connecting any platform (documented as the first step in `docs/SOCIAL_ADS_SETUP.md`).
- ~~**[code]** AI-generated poster/flyer images + promo video for posts (owner request 2026-09-24: "AI will able to generate content... posters, flyers or videos")~~ **Done same day.** New `src/lib/server/aiContent.ts`: `generatePosterImage()` calls OpenAI's `gpt-image-1` (real API key entered by the owner, saved via the established secure-credential path) with the admin's typed description plus the business's real name/city auto-injected -- never invents a promo/price beyond what was typed. Result saved through a new `saveGeneratedFile()` helper in `upload.ts` (same Blob-vs-local-disk strategy as every other upload). New `src/lib/client/videoSlideshow.ts`: a real Ken-Burns pan/zoom slideshow assembled **entirely client-side** (Canvas 2D + MediaRecorder, no server-side ffmpeg, no serverless duration/size limits, no extra API cost) from 1-4 admin-picked images with a text overlay, producing a real short `.webm` file. Explicitly **not** "true" AI-generated video (no model invents footage) -- chosen deliberately over a real video-generation API (Runway/Veo-style) after asking the owner, since that's a much larger, costlier, slower build; scoped this way per their own choice. `MarketingPost` gained a `videoUrl` field (additive migration `20260924050000_marketing_ai_content`) -- reference/download only, no publish API uses it yet (Meta's publish code only handles images).
  - **Real bug found + fixed while live-verifying**: the posts API's `mediaUrl`/`videoUrl` validation (`z.string().url()`) rejected the root-relative `/uploads/...` paths the local-disk upload fallback returns (used whenever `BLOB_READ_WRITE_TOKEN` isn't set, e.g. local dev/testing) -- real production (Vercel, Blob configured) never hit this since Blob URLs are always absolute, so it was invisible there, but it would have silently broken attaching *any* image (manually uploaded or AI-generated) to a post in any environment without Blob configured. Fixed with a validator that accepts either an absolute URL or a root-relative path, matching what the upload endpoints can actually return.
  - **Live-verified with one real, deliberate OpenAI API call** (not mocked, costs real money -- run once and reused for subsequent checks to avoid double-billing): a genuine ~1.76MB 1024x1024 PNG was generated, decoded, saved, and attached to a real post, confirmed via direct PNG-header + file-size checks. Also verified: permission gating (API + validation), the OpenAI key shows configured-and-masked in Settings, and a fake/corrupt "video" buffer is correctly rejected by magic-byte sniffing rather than trusted by MIME type alone.
  - **Live-verified the video pipeline with a real Playwright browser** (Canvas/MediaRecorder can't be exercised from plain Node): uploaded a real image, generated a real slideshow video end-to-end in a real Chromium tab, confirmed a genuine ~270KB `.webm` file with a valid EBML header was produced and persisted as the post's real `videoUrl` in the database -- not a mock, a real browser-recorded video file.
  - **Noted, not chased**: this local dev server's pre-existing, already-documented limitation (`next start` doesn't serve files written to `public/uploads/` after the build completed) meant a freshly-uploaded slide image's URL wasn't fetchable by the browser's own canvas during the Playwright test; routed around it by pointing that one test's upload response at an already-build-time-static asset rather than re-litigating the known limitation. Real production (Vercel + Blob) doesn't have this issue.
- Remaining Phase 7 items (Google Business Profile posting, Search Console) unchanged: need their own developer-app registration before any code is useful; not requested by the owner this pass.

## Phase 8 — Forecasting, optimization, hardening, load tests, staged rollout

**Status: not started**, and not meaningfully startable yet — load testing and forecasting need either real production traffic history or a much larger built surface than exists today. The one piece of "hardening" that *is* actionable now, independent of the rest:
- ~~**[code]** Observability~~ **Sentry wired 2026-09-21** (`docs/OPERATIONS.md` §5, `docs/SECURITY.md` §7a) — code-complete, verified as a safe no-op without a DSN. **[creds]** now: just `NEXT_PUBLIC_SENTRY_DSN`.
- ~~**[code]** Durable rate limiting (Upstash Redis or similar)~~ **Done 2026-09-23** (`7051d2b`) — `checkRateLimit` now uses Upstash Redis when `upstashRedisUrl`/`upstashRedisToken` are set in Settings → Integrations (same DB-configurable pattern as every other credential), falls back to the existing in-memory limiter otherwise, and falls back again (not fails) if Upstash is configured but unreachable. **[creds]** now: the owner adding a real Upstash URL/token whenever they want durability across restarts/instances — code is complete and live-verified against a mock server either way.

## Suggested near-term order (revenue/risk-adjusted, not phase-numbered)

0. **[decision]** Point `abbiescleanco.com` at the live Vercel deployment (`https://abbiescleanco.vercel.app/`, confirmed live and up to date 2026-09-22 — see `docs/DECISIONS.md`'s correction note and `docs/OPERATIONS.md` §1), or explicitly decide not to yet. Right now the real domain still serves a separate, older WordPress site — none of this rebuild is visible to an actual visitor of `abbiescleanco.com` until this is done. This is the single highest-leverage decision on this whole list: everything below is pointless to a real customer until this happens, and it's entirely the owner's call (it takes the current live site down the moment it's done), not something to do without explicit go-ahead.
1. **[creds] key entered 2026-09-23, blocked one step short**: Stripe rejects Checkout until the owner sets an account/business name at dashboard.stripe.com/account (see Phase 3 note above). Highest revenue impact of anything code-side; the code is already written and proven correct up to that point.
2. ~~`middleware.ts`/proxy admin backstop + Sentry wiring~~ **Done 2026-09-21** — both code-complete and live-verified; only Sentry still needs a DSN from the owner.
3. ~~**[creds]** Anthropic key~~ **Done 2026-09-23** — AI photo estimation fully live (see Phase 6 note above).
4. **[creds]** `NEXT_PUBLIC_SENTRY_DSN` → real error visibility, code already proven safe either way.
5. ~~**[creds]** Google Maps/Places API key~~ **Done 2026-09-23** — real prospecting search is live (see Phase 7 note above).
6. ~~**[decision]** Invoicing/accounting approach~~ **Resolved 2026-09-24** — hand-built Finance module chosen over QuickBooks sync, Phase 1 built (see Phase 3 above). Workforce/subcontractor classification (Phase 4) remains open and still gates real further scope (blocks Finance's own Subcontractor payables page too).
7. **[decision]** New-homeowner data source for prospecting (Phase 7) — Google Places can't do this; needs a real-estate/property-records provider decision.
8. Everything else in Phases 5–8, in the order the owner cares about it — none of it matters until #0 happens.
