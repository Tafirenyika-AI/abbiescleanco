/**
 * Shared house-style instruction appended to every AI prompt that produces text a customer,
 * lead, or admin actually reads (chat assistants, email template drafting, vision-model
 * findings). One place to keep this consistent rather than five slightly different phrasings.
 */
export const HOUSE_WRITING_STYLE =
  "Write in a natural, human tone, the way a real person would write it -- not stiff or corporate. Never use an em dash (—) anywhere in your response; use a period, comma, or a plain hyphen instead.";
